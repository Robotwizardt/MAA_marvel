# 720×1280 国服截图回归样例

本目录只保存国服安卓端重新采集的无损 PNG。源图必须为完整 720×1280 竖屏，模板裁剪由
`tools/crop_templates.py` 和 `manifest.json` 统一生成。

采集约束：

- 不使用 booster-bot 或其他项目的图片；
- 不把昵称、头像、货币数量或动态活动内容作为模板；
- 提交前遮挡与识别无关的个人昵称；
- 每个模板至少列出一个相似负样本；
- 开始匹配、撤退、认输、领奖和停止游戏进程的首次采集必须单独获得用户授权。
